# Dependencies
node_modules/

# Build artifacts (compiled JS, source maps, type definitions)
# We ignore this in Git because it's generated code,
# but it must be INCLUDED in the published npm package.
dist/

# Optional: if you use a 'build' or 'out' directory name instead of 'dist'
# build/
# out/

# Logs and temporary files
*.log
npm-debug.log*
.npm/

# Coverage reports
coverage/
.nyc_output/

# Environment files
.env
.env.local

# OS and IDE specific files
.DS_Store           # macOS
Thumbs.db           # Windows
.vscode/            # VS Code configuration (optional, can be shared)
.idea/              # WebStorm/IntelliJ configuration
